conda create --name pytorch_vicar -y && conda activate pytorch_vicar && conda install spyder numpy matplotlib pandas scipy scikit-learn scikit-image -y && conda install pytorch torchvision torchaudio cudatoolkit=11.0 -c pytorch -y && conda install -c simpleitk simpleitk && conda install -c conda-forge opencv -y


## for ubuntu:
conda innstall pip -y
pip install opencv-python
